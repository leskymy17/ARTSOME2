---
/**
 * Karta služby — používá se v mřížce na homepage.
 * Celá karta je klikatelná a vede na detail služby.
 * Fotka přes celou kartu + tmavě modrý gradient zespodu pro čitelnost bílého textu.
 */
import { Image } from 'astro:assets';
import type { Service } from '../data/services';
import Icon from './Icon.astro';

interface Props {
  service: Service;
}

const { service } = Astro.props;
---

<article class="svc reveal">
  <div class="svc__img">
    <Image
      src={service.image}
      alt={service.imageAlt}
      width={480}
      widths={[320, 480, 640]}
      sizes="(max-width: 640px) 92vw, (max-width: 1000px) 46vw, 280px"
    />
    <div class="svc__scrim" aria-hidden="true"></div>
  </div>
  <div class="svc__body">
    <h3 class="svc__title">
      <a href={`/sluzby/${service.slug}/`} class="svc__link-cover">{service.title}</a>
    </h3>
    <span class="svc__more">
      Více o službě <Icon name="arrow-right" size={18} />
    </span>
  </div>
</article>

<style>
  .svc {
    position: relative;
    border-radius: var(--radius);
    overflow: hidden;
    display: flex;
    flex-direction: column;
    min-height: 396px;
    isolation: isolate;
    transition:
      transform .35s var(--ease),
      box-shadow .35s var(--ease);
  }

  .svc:hover {
    transform: translateY(-6px);
    box-shadow: var(--shadow);
  }

  /* Oříznutí zaoblení přímo na obalu fotky, který se škáluje při hoveru.
     Samotné overflow: hidden na .svc občas při současné transformaci karty
     (translateY, reveal) a fotky (scale) v prohlížeči povolí a rohy se na chvíli ukážou hranaté.
     clip-path je na .svc__img, ne na .svc, aby neřezal box-shadow karty. */
  .svc__img {
    position: absolute;
    inset: 0;
    z-index: -1;
    border-radius: inherit;
    overflow: hidden;
    clip-path: inset(0 round var(--radius));
  }

  .svc__img :global(img) {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform .5s var(--ease);
  }

  .svc:hover .svc__img :global(img) { transform: scale(1.07); }

  .svc__scrim {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      to top,
      rgba(14, 18, 82, .95) 0,
      rgba(14, 18, 82, .95) 32px,
      rgba(14, 18, 82, .74) 87px,
      rgba(14, 18, 82, .36) 135px,
      rgba(14, 18, 82, 0) 183px
    );
  }

  /* Bez position: relative — ::after odkazu se má roztáhnout přes celou kartu (.svc), ne jen přes tělo. */
  .svc__body {
    margin-top: auto;
    padding: 20px 20px 22px;
    display: flex;
    flex-direction: column;
    gap: var(--sp-3);
  }

  .svc__title {
    font-size: clamp(1.9rem, 2.1vw, 2.6rem);
    font-weight: var(--fw-bold);
    color: #fff;
  }

  /* Klikatelná celá karta bez zanoření interaktivních prvků */
  .svc__link-cover::after {
    content: '';
    position: absolute;
    inset: 0;
  }

  .svc__more {
    font-family: var(--font-head);
    font-weight: 600;
    font-size: .85rem;
    color: #fff;
    display: inline-flex;
    align-items: center;
    gap: .4rem;
  }

  .svc__more :global(svg) { transition: transform .25s var(--ease); }
  .svc:hover .svc__more :global(svg) { transform: translateX(4px); }
</style>
